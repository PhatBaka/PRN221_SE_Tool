@page
@using SETool_Data.Models.DTOs;
@model SETool_RazorPage.Pages.LoginPageModel

@{
    ViewData["Title"] = "LoginPage";
}

<h1>Login</h1>

<h4>User</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="loginViewModel.Email" class="control-label"></label>
                <input asp-for="loginViewModel.Email" class="form-control" />
                <span asp-validation-for="loginViewModel.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="loginViewModel.Password" class="control-label"></label>
                <input asp-for="loginViewModel.Password" class="form-control" />
                <span asp-validation-for="loginViewModel.Password" class="text-danger"></span>
            </div>
            @* <div class="form-group">
                <label asp-for="loginViewModel.Role" class="control-label"></label>
                <select asp-for="loginViewModel.Role.Id" class="form-control"
                        asp-items="@Model.Roles">
                    <option value="">-- Select role --</option>
                </select>
                <span asp-validation-for="loginViewModel.Role.Id" class="text-danger" />
            </div> *@
            <div class="form-group">
                <input type="submit" value="Submit" asp-page-handler="Submit"  class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
